# Database
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/egc}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:mknic123}
spring.jpa.hibernate.ddl-auto=update
server.port=${SERVER_PORT:8080}

# File upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB
file.upload-dir=${FILE_UPLOAD_DIR:uploads}

# JSP view resolver
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# JWT
jwt.secret=${JWT_SECRET:egc-child-protection-system-secret-key-change-me-in-production-1234567890}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# Cookie security — SameSite=Lax mitigates CSRF for the JWT-in-cookie approach
server.tomcat.cookie.same-site-cookie-value=Lax

# Mail / SMTP
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:anc.official.dev@gmail.com}
spring.mail.password=${MAIL_PASSWORD:znba ehze pexu nbdi}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=${MAIL_FROM:anc.official.dev@gmail.com}
